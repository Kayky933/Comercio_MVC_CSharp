@model Mercado.MVC.Models.ProdutoModel

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout-Interno.cshtml";
}

<h1>Editar</h1>

<h4>Produto</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <input type="hidden" asp-for="Id_Usuario" value="@ViewBag.usuarioId" />
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Descricao" class="control-label"></label>
                <input asp-for="Descricao" class="form-control" />
                <span asp-validation-for="Descricao" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Preco_Unidade" class="control-label"></label>
                <input asp-for="Preco_Unidade" class="form-control" />
                <span asp-validation-for="Preco_Unidade" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Unidade_De_Medida" class="control-label"></label>
                <select asp-for="Unidade_De_Medida" asp-items="@ViewBag.UnidaDeMedida" class="form-control"></select>
                <span asp-validation-for="Unidade_De_Medida" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Id_Categoria" class="control-label"></label>
                <select asp-for="Id_Categoria" class="form-control" asp-items="ViewBag.Id_Categoria"></select>
                <span asp-validation-for="Id_Categoria" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Voltar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

<script>
    $.validator.methods.number = function (value, element) {
        return this.optional(element) || /^-?(?:\d+|\d{1,3}(?:\.\d{3})+)?(?:,\d+)?$/.test(value);
    }
</script>
}
